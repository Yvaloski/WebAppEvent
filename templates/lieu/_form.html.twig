{{ form_start(form, {'attr': {'action': path('app_lieu_new'), 'id': 'lieuForm'}}) }}
    {{ form_widget(form) }}
    <button class="btn" type="submit">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
