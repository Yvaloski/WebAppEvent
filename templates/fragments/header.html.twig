<header>
    <nav class="navbar navbar-expand-lg " >
        <div class="container-fluid" >
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <a href="{{ path('app_sortie_index') }}">
                    <img src="{{ asset('images/logo eni.png') }}" alt="" width="80px"></a>
                    <ul class="navbar-nav">

                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link text-light" href="{{ path('app_sortie_index') }}">Accueil
                                    <span class="visually-hidden">(current)</span>
                                </a>
                            </li>
                        {% endif %}

                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="nav-item">
                                <a class="nav-link text-light " href="{{ path('admin')}}">Espace Admin
                                    <span class="visually-hidden">(current)</span>
                                </a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
                        {% if app.user %}
                            <a href="{{ path('app_sortie_new') }}" id="creation" class="btn  buttonBlancBleu m-3">Créer Sortie</a>
                            <p class="text-light">{{ app.user.fullName }}</p>
                            <ul>

                                <li class="nav-item list-unstyled">

                                    <a href=""><svg xmlns="http://www.w3.org/2000/svg" height="2em" viewBox="0 0 448 512"><!--! Font Awesome Free 6.4.2 by @fontawesome - https://fontawesome.com License - https://fontawesome.com/license (Commercial License) Copyright 2023 Fonticons, Inc. --><style>svg{fill:#ffffff}</style><path d="M224 256A128 128 0 1 0 224 0a128 128 0 1 0 0 256zm-45.7 48C79.8 304 0 383.8 0 482.3C0 498.7 13.3 512 29.7 512H418.3c16.4 0 29.7-13.3 29.7-29.7C448 383.8 368.2 304 269.7 304H178.3z"/></svg></a>
                                    <ul>
                                        <li><a href="{{ path('app_user_show', {'id': app.user.id}) }}">Mon profil</a></li>
                                        <li><a href="{{ path('app_logout') }}">Deconnexion</a></li>

                                    </ul>
                                </li>
                            </ul>
                        {% endif %}
            </div>
    </nav>

</header>